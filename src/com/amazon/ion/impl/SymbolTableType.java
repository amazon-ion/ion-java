package com.amazon.ion.impl;

/**
 * Enumeration defining the types of symbol tables.
 * (this is a rotten spot to put this enum, per
 */
public enum SymbolTableType {
	SYSTEM, 
	LOCAL, 
	SHARED, 
	INVALID 
}
